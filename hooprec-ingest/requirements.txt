# Basketball RAG Ingestion — Python dependencies
# Install with: pip install -r requirements.txt
# Then run:     playwright install chromium

crawl4ai[all]>=0.4.0
playwright>=1.44.0
aiohttp>=3.9.0
aiosqlite>=0.20.0
aiofiles>=23.0.0
python-dotenv>=1.0.0
tqdm>=4.66.0
